function printCity(name, population, treasury) {
    const cityRecord = {};
    cityRecord.name = name;
    cityRecord.population = population;
    cityRecord.treasury = treasury;

    return cityRecord;
}
